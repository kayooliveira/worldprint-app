import React, { Component } from 'react';

import * as B from "react-bootstrap";

const ProductForm = ({title,fields,isEdit}) => {
    return ( 
        
     );
}
 
export default ProductForm;{title,fields,isEdit}